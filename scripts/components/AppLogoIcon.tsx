/* 
  AppLogoIcon.tsx
  A tiny custom component to have a central place to change the middle component in the header. 
  This is used on all screens aside from the log in.
*/

import React from "react";
import { Text } from "react-native"

export const AppLogoIcon = <Text style={{color: "white"}}>Litter Tracker</Text>
    